---
description: Add more details and threads to an existing writing kernel
argument-hint: kernel filename (e.g. intensity-social-feedback)
---

Read the kernel at `_ideas/writing/$ARGUMENTS.md`. Summarize what's already captured -- the core idea, any threads listed, and any notes added since capture. Then ask one open question to draw out more raw material. Keep it conversational, one question at a time. When Marshall shares new thoughts, reflect them back and ask the next question. When there's enough new material, append it to the idea doc under a dated `## additional threads (YYYY-MM-DD)` section. Never overwrite existing content.
